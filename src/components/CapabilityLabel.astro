---
import { getCollection } from 'astro:content';

interface Props {
  track: 'core' | 'manager';
  capability: string;
}

const capabilities = await getCollection('capabilities');
const { track, capability } = Astro.props;
---

{capabilities.find((c) => c.data.id === `${track}-${capability}`)?.data.title ?? capability}
